# Git revert
Git revert is safe for shared branches and it keeps the history intact.

# Git reset --hard
moves HEAD backwards
Deletes commits from the branch history

# Git reflog
used for recording HEAD movement
It can also be used to recover commits after pushing